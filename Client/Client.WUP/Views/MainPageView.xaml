<Page
    x:Class="Client.WUP.Views.MainPageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.WUP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:Client.WUP.UserControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <StackPanel Margin="0,55,0,0" Orientation="Horizontal" HorizontalAlignment="Center" Background="Aqua">
                <Button Margin="10" Click="{x:Bind viewModel.GoToFeed}" Background="Transparent">
                    <Image Source="/Assets/document.png" Height="30"/>
                </Button>
                <Button Margin="10" Click="{x:Bind viewModel.GoToIdentity}">
                    <Image Source="/Assets/pen.png" Height="30"/>
                </Button>
                <Button Margin="10" Click="{x:Bind viewModel.logOut}" Background="Transparent">
                    <Image Source="/Assets/key.png" Height="30"/>
                </Button>
            </StackPanel>
            <StackPanel Margin="15" x:Name="contentSP" VerticalAlignment="Center" HorizontalAlignment="Center">
                <uc:FeedUserControl>
                </uc:FeedUserControl>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
