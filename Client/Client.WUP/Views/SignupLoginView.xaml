<Page
    x:Class="Client.WUP.Views.SignupLoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.WUP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="15" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Margin="10" Orientation="Horizontal">
                <TextBlock Margin="5" Text="Username"/>
                <TextBox Text="{x:Bind viewModel.Username, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Margin="10" Orientation="Horizontal">
                <TextBlock Margin="5" Text="Password"/>
                <TextBox Margin="5" Text="{x:Bind viewModel.Password, Mode=TwoWay}"/>
            </StackPanel>
            <Button Margin="10" Content="Signp" Click="{x:Bind viewModel.Signup}"/>
            <Button Margin="10" Content="Login" Click="{x:Bind viewModel.Login}"/>
            <Button Click="{x:Bind viewModel.LoginWithFacebook}">
                <Image Margin="10" Source="/Assets/facebook.jpg" Height="100" Width="300" />
            </Button>
            <TextBlock Margin="10" Text="{x:Bind viewModel.Message, Mode=OneWay}"/>
        </StackPanel>
    </Grid>
</Page>
